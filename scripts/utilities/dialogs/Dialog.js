class Dialog {
    author;
    authorTitle;
    message;
    trigger;

    constructor(author, authorTitle, message, trigger) {
        this.author = author;
        this.authorTitle = authorTitle
        this.message = message
        this.trigger = trigger
    }
}